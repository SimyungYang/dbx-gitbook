---
title: "Lab 25.10 — MLflow Feedback API"
---

<Info>
**소요 시간**: 10분 | **난이도**: ⭐⭐
**선행**: TBD
**기능 분류**: mlflow_python
</Info>

## 🧭 이 기능은 무엇이고 언제 쓰나

본 Lab은 **MLflow Feedback API** 을 단일 기능 단위로 직접 수행하며 동작을 체감하는 것이 목적입니다. `M25. MLflow — Tracking / Tracing / Evaluation` 의 핵심 흐름 안에서 이 단계가 어떤 역할을 하는지 위치를 잡습니다.

## ⚙️ 핵심 옵션 / 파라미터

| 옵션 | 설명 |
|------|------|
| 본 Lab에서 다루는 핵심 변수 | 샘플 코드의 `<...>` 부분을 본인 환경에 맞게 교체 |

## 💻 샘플 코드

```python
import mlflow
import mlflow.sklearn
from sklearn.linear_model import LogisticRegression
from sklearn.datasets import load_iris

mlflow.autolog()

X, y = load_iris(return_X_y=True)
with mlflow.start_run() as run:
    model = LogisticRegression(max_iter=200).fit(X, y)
    print('Run ID:', run.info.run_id)

# UC Models에 등록
mlflow.register_model(
    model_uri=f'runs:/{run.info.run_id}/model',
    name='workshop_<name>.default.iris_clf',
)
```

## 🖥 UI 등가 작업 — 스크린샷 위치

**Step 1.** ML → Experiments에서 Run 확인

![MLflow Feedback API — Step 1](/images/dbx-workshop/25-mlflow/25-10/step1-experiment.png)

**Step 2.** Catalog Explorer → workshop_<name>.default → Models

![MLflow Feedback API — Step 2](/images/dbx-workshop/25-mlflow/25-10/step2-uc-models.png)

**Step 3.** Model 버전 → Alias 부여 (@champion)

![MLflow Feedback API — Step 3](/images/dbx-workshop/25-mlflow/25-10/step3-alias.png)

## ✅ Validation

- [ ] MLflow Feedback API 완료
- [ ] 결과가 기대한 대로 표시됨

## 🚧 자주 만나는 오류

| 에러 | 원인 | 해결 |
|------|------|------|
| `권한 부족` | 필요 권한 미보유 | 선행 Lab 확인 또는 Admin 문의 |

## 🧹 Cleanup

Module 끝의 Cleanup Lab에서 일괄 정리합니다.

