---
title: "Lab 40.4 — Code Lineage 확인"
---

<Info>
**소요 시간**: 10분 | **난이도**: ⭐⭐
**선행**: TBD
**기능 분류**: testing
</Info>

## 🧭 이 기능은 무엇이고 언제 쓰나

본 Lab은 **Code Lineage 확인** 을 단일 기능 단위로 직접 수행하며 동작을 체감하는 것이 목적입니다. `M40. Testing & Quality` 의 핵심 흐름 안에서 이 단계가 어떤 역할을 하는지 위치를 잡습니다.

## ⚙️ 핵심 옵션 / 파라미터

| 옵션 | 설명 |
|------|------|
| 본 Lab에서 다루는 핵심 변수 | 샘플 코드의 `<...>` 부분을 본인 환경에 맞게 교체 |

## 💻 샘플 코드

```python
# tests/test_transform.py
import pytest
from notebooks.transform import clean

def test_clean_drops_null():
    df = spark.createDataFrame([(1, 'a'), (2, None)], ['id', 'name'])
    out = clean(df)
    assert out.count() == 1

# 실행: pytest tests/ (Databricks Connect로 원격 cluster 활용)
```

## 🖥 UI 등가 작업 — 스크린샷 위치

**Step 1.** 로컬에 PyTest + Databricks Connect 환경

![Code Lineage 확인 — Step 1](/images/dbx-workshop/40-testing-quality/40-4/step1-setup.png)

**Step 2.** `pytest tests/` 실행 → 모든 케이스 PASS

![Code Lineage 확인 — Step 2](/images/dbx-workshop/40-testing-quality/40-4/step2-run-tests.png)

## ✅ Validation

- [ ] Code Lineage 확인 완료
- [ ] 결과가 기대한 대로 표시됨

## 🚧 자주 만나는 오류

| 에러 | 원인 | 해결 |
|------|------|------|
| `권한 부족` | 필요 권한 미보유 | 선행 Lab 확인 또는 Admin 문의 |

## 🧹 Cleanup

Module 끝의 Cleanup Lab에서 일괄 정리합니다.

