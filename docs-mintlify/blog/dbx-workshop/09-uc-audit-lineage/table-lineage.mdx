---
title: "Lab 9.4 — Table Lineage 자동 추적"
---

<Info>
**소요 시간**: 10분 | **난이도**: ⭐⭐
**선행**: TBD
**기능 분류**: uc_audit
</Info>

## 🧭 이 기능은 무엇이고 언제 쓰나

본 Lab은 **Table Lineage 자동 추적** 을 단일 기능 단위로 직접 수행하며 동작을 체감하는 것이 목적입니다. `M9. UC Audit & Lineage` 의 핵심 흐름 안에서 이 단계가 어떤 역할을 하는지 위치를 잡습니다.

## ⚙️ 핵심 옵션 / 파라미터

| 옵션 | 설명 |
|------|------|
| 본 Lab에서 다루는 핵심 변수 | 샘플 코드의 `<...>` 부분을 본인 환경에 맞게 교체 |

## 💻 샘플 코드

```sql
-- 최근 1일 audit 이벤트
SELECT event_time, user_identity.email, action_name, request_params
FROM system.access.audit
WHERE event_date >= current_date() - 1
ORDER BY event_time DESC
LIMIT 100;

-- Table Lineage 조회
SELECT * FROM system.access.table_lineage
WHERE target_table_full_name = 'workshop_<name>.default.customers';
```

## 🖥 UI 등가 작업 — 스크린샷 위치

**Step 1.** SQL Editor → system.access 쿼리

![Table Lineage 자동 추적 — Step 1](/images/dbx-workshop/09-uc-audit-lineage/9-4/step1-system-access.png)

**Step 2.** Catalog Explorer → 테이블 → Lineage 탭

![Table Lineage 자동 추적 — Step 2](/images/dbx-workshop/09-uc-audit-lineage/9-4/step2-lineage-ui.png)

## ✅ Validation

- [ ] Table Lineage 자동 추적 완료
- [ ] 결과가 기대한 대로 표시됨

## 🚧 자주 만나는 오류

| 에러 | 원인 | 해결 |
|------|------|------|
| `권한 부족` | 필요 권한 미보유 | 선행 Lab 확인 또는 Admin 문의 |

## 🧹 Cleanup

Module 끝의 Cleanup Lab에서 일괄 정리합니다.

