---
title: "Lab 29.1 — Lakebase Instance 생성"
---

<Info>
**소요 시간**: 10분 | **난이도**: ⭐⭐
**선행**: TBD
**기능 분류**: lakebase
</Info>

## 🧭 이 기능은 무엇이고 언제 쓰나

본 Lab은 **Lakebase Instance 생성** 을 단일 기능 단위로 직접 수행하며 동작을 체감하는 것이 목적입니다. `M29. Lakebase — Postgres OLTP + 권한` 의 핵심 흐름 안에서 이 단계가 어떤 역할을 하는지 위치를 잡습니다.

## ⚙️ 핵심 옵션 / 파라미터

| 옵션 | 설명 |
|------|------|
| 본 Lab에서 다루는 핵심 변수 | 샘플 코드의 `<...>` 부분을 본인 환경에 맞게 교체 |

## 💻 샘플 코드

```bash
# psql 클라이언트로 연결
psql -h <lakebase-host> -p 5432 -U <user> -d <database> -W

# SQL
CREATE TABLE app_state (
  id BIGSERIAL PRIMARY KEY,
  data JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

## 🖥 UI 등가 작업 — 스크린샷 위치

**Step 1.** Compute → Lakebase → Create instance

![Lakebase Instance 생성 — Step 1](/images/dbx-workshop/29-lakebase/29-1/step1-create-lb.png)

**Step 2.** Database 생성 (UI 또는 psql)

![Lakebase Instance 생성 — Step 2](/images/dbx-workshop/29-lakebase/29-1/step2-create-db.png)

**Step 3.** Connection string 복사 → psql 연결

![Lakebase Instance 생성 — Step 3](/images/dbx-workshop/29-lakebase/29-1/step3-connect.png)

## ✅ Validation

- [ ] Lakebase Instance 생성 완료
- [ ] 결과가 기대한 대로 표시됨

## 🚧 자주 만나는 오류

| 에러 | 원인 | 해결 |
|------|------|------|
| `권한 부족` | 필요 권한 미보유 | 선행 Lab 확인 또는 Admin 문의 |

## 🧹 Cleanup

Module 끝의 Cleanup Lab에서 일괄 정리합니다.

