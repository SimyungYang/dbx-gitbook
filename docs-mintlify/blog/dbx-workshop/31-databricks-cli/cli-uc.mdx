---
title: "Lab 31.4 — UC 객체 다루기 (catalog/schema/table)"
---

<Info>
**소요 시간**: 10분 | **난이도**: ⭐⭐
**선행**: TBD
**기능 분류**: cli_bash
</Info>

## 🧭 이 기능은 무엇이고 언제 쓰나

본 Lab은 **UC 객체 다루기 (catalog/schema/table)** 을 단일 기능 단위로 직접 수행하며 동작을 체감하는 것이 목적입니다. `M31. Databricks CLI` 의 핵심 흐름 안에서 이 단계가 어떤 역할을 하는지 위치를 잡습니다.

## ⚙️ 핵심 옵션 / 파라미터

| 옵션 | 설명 |
|------|------|
| 본 Lab에서 다루는 핵심 변수 | 샘플 코드의 `<...>` 부분을 본인 환경에 맞게 교체 |

## 💻 샘플 코드

```bash
# UC 객체 다루기 (catalog/schema/table)
# Databricks CLI 명령
databricks --version

# 예: workspace, clusters, jobs, sql, apps 등 서브 명령
databricks workspace list /
databricks clusters list --output json | jq '.[] | {name, state}'
```

## 🖥 UI 등가 작업 — 스크린샷 위치

**Step 1.** 터미널에서 `databricks --help` 실행

![UC 객체 다루기 (catalog/schema/table) — Step 1](/images/dbx-workshop/31-databricks-cli/31-4/step1-cli-help.png)

**Step 2.** 해당 서브 명령 실행 후 결과 비교

![UC 객체 다루기 (catalog/schema/table) — Step 2](/images/dbx-workshop/31-databricks-cli/31-4/step2-subcommand.png)

## ✅ Validation

- [ ] UC 객체 다루기 (catalog/schema/table) 완료
- [ ] 결과가 기대한 대로 표시됨

## 🚧 자주 만나는 오류

| 에러 | 원인 | 해결 |
|------|------|------|
| `권한 부족` | 필요 권한 미보유 | 선행 Lab 확인 또는 Admin 문의 |

## 🧹 Cleanup

Module 끝의 Cleanup Lab에서 일괄 정리합니다.

