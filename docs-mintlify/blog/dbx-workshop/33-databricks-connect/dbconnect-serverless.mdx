---
title: "Lab 33.6 — Serverless Compute에 Connect"
---

<Info>
**소요 시간**: 10분 | **난이도**: ⭐⭐
**선행**: TBD
**기능 분류**: dbconnect
</Info>

## 🧭 이 기능은 무엇이고 언제 쓰나

본 Lab은 **Serverless Compute에 Connect** 을 단일 기능 단위로 직접 수행하며 동작을 체감하는 것이 목적입니다. `M33. Databricks Connect — 로컬 IDE에서 원격 Spark` 의 핵심 흐름 안에서 이 단계가 어떤 역할을 하는지 위치를 잡습니다.

## ⚙️ 핵심 옵션 / 파라미터

| 옵션 | 설명 |
|------|------|
| 본 Lab에서 다루는 핵심 변수 | 샘플 코드의 `<...>` 부분을 본인 환경에 맞게 교체 |

## 💻 샘플 코드

```python
from databricks.connect import DatabricksSession

spark = DatabricksSession.builder.profile('workshop').getOrCreate()

df = spark.table('samples.tpch.customer')
df.show(5)
```

## 🖥 UI 등가 작업 — 스크린샷 위치

**Step 1.** 로컬 venv에서 `pip install databricks-connect`

![Serverless Compute에 Connect — Step 1](/images/dbx-workshop/33-databricks-connect/33-6/step1-install.png)

**Step 2.** `databricks configure --profile workshop` (PAT)

![Serverless Compute에 Connect — Step 2](/images/dbx-workshop/33-databricks-connect/33-6/step2-configure.png)

**Step 3.** 위 Python 코드 실행

![Serverless Compute에 Connect — Step 3](/images/dbx-workshop/33-databricks-connect/33-6/step3-run-local.png)

## ✅ Validation

- [ ] Serverless Compute에 Connect 완료
- [ ] 결과가 기대한 대로 표시됨

## 🚧 자주 만나는 오류

| 에러 | 원인 | 해결 |
|------|------|------|
| `권한 부족` | 필요 권한 미보유 | 선행 Lab 확인 또는 Admin 문의 |

## 🧹 Cleanup

Module 끝의 Cleanup Lab에서 일괄 정리합니다.

